// Entry point of the application.
// Here we import the main.css file and create the Vue app instance.
import './assets/main.css'

import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
